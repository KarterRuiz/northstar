/** Starting blank rows on the multi-student grid. */
export const BULK_ADD_INITIAL_ROWS = 5;

/** Rows added by “Add 5 more rows”. */
export const BULK_ADD_ROW_BATCH = 5;

/**
 * Client-side max rows for v1. Keeps the grid responsive and
 * avoids oversized single-request creates. Use Import roster for larger sets.
 */
export const BULK_ADD_MAX_ROWS = 50;

export const BULK_ADD_NAME_MAX = 120;
export const BULK_ADD_EXTERNAL_ID_MAX = 64;
